(defun prn (n)
  (format t "~&press ~s" n))

(require :incudine)

(in-package :scratch)

(rt-start)

(at (+ (now) 44100) #'prn 15)

(defun gliss (first-key last-key spread)
  (cond ((= first-key last-key) 0)
	(t (prn first-key)
	   (at (+ (now) spread) #'gliss (1+ first-key) last-key spread))))

